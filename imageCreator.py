from random import randrange
import struct
from tkinter import PhotoImage
def createPPM(characterData):

    return imgData
    #imageID = canvas.create_image(canvas.canvasx(0), canvas.canvasy(0), image=canvasImage, anchor='nw')
